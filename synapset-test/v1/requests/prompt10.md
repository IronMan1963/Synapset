# Pair 10: StyledPage

## Original Prompt 10: StyledPage

```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Assemble a complete landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```

#### Variant 10.1
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Compose a complete landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.2
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Assemble a full landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.3
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Assemble a complete landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Maintain the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.4
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Assemble a complete landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Alter the provided hero or navigation markup".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.5
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Assemble a complete landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Produce a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.6
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Build a complete landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context components"; "Employ semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.7
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Construct a complete landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation code".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.8
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Assemble a complete landing page from provided components", "adding a feature grid and footer to finish the layout".
You must: "Keep the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.9
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Create a complete landing page from provided components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context components"; "Use semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Output a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 10.10
```
This is StyledPage.
Accepts palette = "blue and white" as parameters.
The given context is HeroSection and NavBar.
My problem is "Assemble a complete landing page from supplied components", "adding a feature grid and footer to complete the layout".
You must: "Preserve the structure of the context elements"; "Utilize semantic HTML5 tags throughout".
You must not: "Modify the provided hero or navigation markup".
The output must: "Be a single valid HTML5 document"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```