# Pair 1: EcommerceLanding

## Original Prompt 1: EcommerceLanding

```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Generate a responsive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```

#### Variant 1.1
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Create a responsive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.2
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Generate a responsive landing page with a hero section", "a fixed navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.3
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Generate a responsive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Employ semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.4
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Generate a responsive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline CSS styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.5
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Generate a responsive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Produce a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.6
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Build a responsive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Add responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.7
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Produce a responsive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include third-party JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.8
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Generate a responsive landing page with a hero banner", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility standards".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.9
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Design a responsive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Use semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Embed CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```


#### Variant 1.10
```
This is EcommerceLanding.
Accepts style = "modern flat", palette = "blue and white" as parameters.
My problem is "Generate an adaptive landing page with a hero section", "a sticky navigation bar", "a product feature grid", "and a call-to-action button".
You must: "Utilize semantic HTML5 tags"; "Include responsive CSS media queries"; "Follow WCAG 2.1 accessibility guidelines".
You must not: "Use inline styles"; "Include external JavaScript frameworks".
The output must: "Be a single valid HTML5 document"; "Include embedded CSS in a style tag"; "use ", style, " design aesthetics"; "apply a ", palette, " colour scheme".
Generate a natural language prompt.
```

