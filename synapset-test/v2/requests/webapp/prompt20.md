# Pair 20: PomodoroTimer

## Original Prompt 20: PomodoroTimer

```
This is PomodoroTimer.
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
My problem is "Generate a single-page Pomodoro timer web application", "with a circular countdown display", "start, pause, and reset controls", "alternating between", workMinutes, "minute work sessions and", breakMinutes, "minute break sessions", "and an audio notification when a session ends".
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic using setInterval"; "Provide a visual progress indicator"; "Update the page title with remaining time".
You must not: "Use inline styles"; "Use external libraries or frameworks".
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
Generate a natural language prompt.
```

#### Variant 20.1
```
This is PomodoroTimer.
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
My problem is "Create a single-page Pomodoro timer web application", "with a circular countdown display", "start, pause, and reset controls", "alternating between", workMinutes, "minute work sessions and", breakMinutes, "minute break sessions", "and an audio notification when a session ends".
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic using setInterval"; "Provide a visual progress indicator"; "Update the page title with remaining time".
You must not: "Use inline styles"; "Use external libraries or frameworks".
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
Generate a natural language prompt.
```


#### Variant 20.2 (shuffled: output before request)
```
This is PomodoroTimer.
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
My problem is "Generate a single-page Pomodoro timer web application", "with a circular countdown display", "start, pause, and reset controls", "alternating between", workMinutes, "minute work sessions and", breakMinutes, "minute break sessions", "and an audio notification when a session ends".
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic using setInterval"; "Provide a visual progress indicator"; "Update the page title with remaining time".
You must not: "Use inline styles"; "Use external libraries or frameworks".
Generate a natural language prompt.
```


#### Variant 20.3 (synonym variation)
```
This is PomodoroTimer.
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
My problem is "Build a focus timer web application using the Pomodoro technique", "featuring a circular countdown visualization", "buttons for starting, pausing, and resetting the timer", "cycling between", workMinutes, "minute focus periods and", breakMinutes, "minute rest periods", "with a sound alert at session completion".
You must: "Employ semantic HTML5 elements"; "Implement JavaScript timer logic via setInterval"; "Display a visual progress ring"; "Reflect remaining time in the browser tab title".
You must not: "Rely on inline styles"; "Depend on external libraries or frameworks".
The output must: "Produce a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
Generate a natural language prompt.
```


#### Variant 20.4 (fully shuffled)
```
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic using setInterval"; "Provide a visual progress indicator"; "Update the page title with remaining time".
You must not: "Use inline styles"; "Use external libraries or frameworks".
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
This is PomodoroTimer.
My problem is "Generate a single-page Pomodoro timer web application", "with a circular countdown display", "start, pause, and reset controls", "alternating between", workMinutes, "minute work sessions and", breakMinutes, "minute break sessions", "and an audio notification when a session ends".
Generate a natural language prompt.
```


#### Variant 20.5 (no name, tag based)
```
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
My problem is "Generate a single-page Pomodoro timer web application", "with a circular countdown display", "start, pause, and reset controls", "alternating between", workMinutes, "minute work sessions and", breakMinutes, "minute break sessions", "and an audio notification when a session ends".
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic using setInterval"; "Provide a visual progress indicator"; "Update the page title with remaining time".
You must not: "Use inline styles"; "Use external libraries or frameworks".
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
Generate a tag based prompt.
```


#### Variant 20.6 (no negative constraints, with context)
```
This is PomodoroTimer.
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
The given context is "Timer is used by software developers during pair programming sessions".
My problem is "Generate a single-page Pomodoro timer web application", "with a circular countdown display", "start, pause, and reset controls", "alternating between", workMinutes, "minute work sessions and", breakMinutes, "minute break sessions", "and an audio notification when a session ends".
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic using setInterval"; "Provide a visual progress indicator"; "Update the page title with remaining time".
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
Generate a natural language prompt.
```


#### Variant 20.7 (internal ordering shuffled)
```
This is PomodoroTimer.
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
My problem is "Generate a single-page Pomodoro timer web application", "and an audio notification when a session ends", "alternating between", workMinutes, "minute work sessions and", breakMinutes, "minute break sessions", "start, pause, and reset controls", "with a circular countdown display".
You must: "Update the page title with remaining time"; "Provide a visual progress indicator"; "Include JavaScript for timer logic using setInterval"; "Use semantic HTML5 elements".
You must not: "Use external libraries or frameworks"; "Use inline styles".
The output must: "use ", style, " design aesthetics"; "Be a single valid HTML5 document with embedded CSS and JavaScript".
Generate a natural language prompt.
```


#### Variant 20.8 (different request structure)
```
This is PomodoroTimer.
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
My problem is "Produce a productivity timer following the Pomodoro method", "rendered as a full-page web application", "showing time remaining in a circular animated gauge", "with controls to start, pause, and restart", "automatically switching between", workMinutes, "minute work and", breakMinutes, "minute break cycles", "playing a notification sound at each transition".
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic using setInterval"; "Provide a visual progress indicator"; "Update the page title with remaining time".
You must not: "Use inline styles"; "Use external libraries or frameworks".
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
Generate a natural language prompt.
```


#### Variant 20.9 (minimal)
```
My problem is "Generate a single-page Pomodoro timer web application", "with a circular countdown display", "start, pause, and reset controls", "alternating between 25 minute work sessions and 5 minute break sessions".
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic".
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use modern flat design aesthetics".
```


#### Variant 20.10 (Use reference to another prompt)
```
This is PomodoroTimer.
Use ModalDialog.
Accepts style = "modern flat", workMinutes = "25", breakMinutes = "5" as parameters.
My problem is "Generate a single-page Pomodoro timer web application", "with a circular countdown display", "start, pause, and reset controls", "alternating between", workMinutes, "minute work sessions and", breakMinutes, "minute break sessions", "and a modal notification when a session ends".
You must: "Use semantic HTML5 elements"; "Include JavaScript for timer logic using setInterval"; "Provide a visual progress indicator"; "Update the page title with remaining time".
You must not: "Use inline styles"; "Use external libraries or frameworks".
The output must: "Be a single valid HTML5 document with embedded CSS and JavaScript"; "use ", style, " design aesthetics".
Generate a natural language prompt.
```
